// import { createContext } from "react";

// type RedisProviderProps = {
//   children: ReactNode;
//   url: string;
// };

// export type RedisContextType = {
//   client: any;
//   // isConnected ...
// };

// export const RedisContext = createContext<RedisContextType | undefined>(undefined);

// export function RedisProvider({ children, url }: RedisProviderProps) {
//   if (!url) {
//     throw new Error("redis url is not defined")
//   }

//   // setState ...

//   const value: RedisContextType = useMemo(() => {
//     return  {

//     }
//   })
// }

export default {};
